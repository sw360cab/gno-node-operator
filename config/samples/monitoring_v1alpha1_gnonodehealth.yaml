apiVersion: monitoring.k8s.gno.land/v1alpha1
kind: GnoNodeHealth
metadata:
  labels:
    app.kubernetes.io/name: gno-node-health
    app.kubernetes.io/managed-by: kustomize
  name: gnonodehealth-sample
spec:
  # TODO(user): Add fields here
